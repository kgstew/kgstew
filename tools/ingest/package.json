{
  "name": "@kgstew/ingest",
  "version": "0.1.0",
  "description": "Asset ingestion for kgstew.com — derivatives, EXIF stripping, Vercel Blob upload",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "ingest": "node --env-file-if-exists=../../.env.local --env-file-if-exists=.env.local ingest.mjs"
  },
  "type": "module",
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "sharp": "^0.35.3",
    "yaml": "^2.9.0"
  },
  "private": true
}
